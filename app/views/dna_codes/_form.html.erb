<%= form_with(model: dna_code) do |form| %>
  <% if dna_code.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dna_code.errors.count, "error") %> prohibited this dna_code from being saved:</h2>

      <ul>
        <% dna_code.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :gene_name, style: "display: block"%>
    <%= form.text_field :gene_name,  class: "form-control" %>
  </div>
  <div>
    <%= form.label :raw, style: "display: block" %>
    <%= form.text_area :raw, class: "form-control" %>
  </div>

  <div>
    <% options = options_from_collection_for_select(Profile.all, 'account_id', 'name') %>
    
    <%= form.label :account_id, "Patient", style: "display: block" %>
    <%= form.select :all_val,  options, class: "form-control" %>
  </div>



  <div>
    <%= form.submit class: "btn btn-dark btn-sm mt-3" %>
  </div>
<% end %>
